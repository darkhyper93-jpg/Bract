# Error / Decision Log

## Format
```
## [YYYY-MM-DD] Título del problema
**Problema:** Descripción del problema encontrado
**Causa:** Por qué ocurrió
**Solución:** Qué se hizo
**Lección:** Qué aprendemos para no repetirlo
```

## [2026-06-07] apps/api "build" script no emite archivos compilados
**Problema:** `apps/api/package.json` define `"build": "tsc --noEmit"`, igual que `typecheck`. La flag `--noEmit` impide que TypeScript emita archivos en `dist/`, lo cual hace imposible ejecutar `node dist/server.js` en producción.
**Causa:** El script `build` fue configurado para validación de tipos, no para compilación real.
**Solución:** Cambiado a `"build": "prisma generate && tsc"` y agregado `"start": "node dist/server.js"`. `prisma generate` corre primero para que el cliente Prisma exista en `node_modules/.prisma/client/` antes de compilar. El script `typecheck` mantiene `tsc --noEmit` para validación sin output.
**Lección:** `typecheck` = validar tipos sin output. `build` = compilar con output. En imágenes Docker limpias, `prisma generate` debe correr antes de `tsc` o el import de `@prisma/client` puede fallar.

## [2026-06-07] nginx:alpine no incluye ngx_headers_more
**Problema:** La especificación usa `more_clear_headers Server;` para eliminar el header `Server` de las respuestas Nginx. Este directivo requiere el módulo `ngx_headers_more`, que no está incluido en la imagen `nginx:1.25-alpine`.
**Causa:** La imagen oficial de Nginx Alpine es minimal; módulos extras requieren compilación o imagen alternativa (nginx-extras, openresty).
**Solución:** Se eliminó la directiva `more_clear_headers`. La directiva `server_tokens off` ya presente oculta la versión de Nginx (ej: `nginx/1.25.3` → `nginx`). Si se necesita eliminar el header `Server` completamente, migrar a `openresty:alpine` o compilar Nginx con `--add-module=ngx_headers_more`.
**Lección:** Verificar disponibilidad de módulos Nginx antes de especificarlos. Las imágenes Alpine son minimal por diseño.

## [2026-06-07] BullMQ reemplazado por ejecución síncrona para MVP
**Problema:** BullMQ requiere conexión TCP persistente (ioredis). El plan Free de Upstash Redis limita a 1 conexión TCP simultánea, causando que los jobs se encolen pero nunca se procesen en producción.
**Causa:** Upstash Free usa un proxy HTTP que no soporta todos los comandos de Redis que BullMQ necesita para mantener workers activos.
**Solución:** Workers convertidos a ejecución síncrona directa: notification.producer.ts crea notificaciones directamente en Prisma; email jobs ya llamaban a Resend directamente (sin cambios); cleanup reemplazado por setInterval nativo en server.ts cada 15 minutos. Los archivos *.worker.ts y queues.ts se conservan sin modificar para reactivar BullMQ fácilmente cuando el tráfico justifique Upstash Pay-as-you-go.
**Lección:** Para MVPs, la ejecución síncrona es preferible a colas si el volumen es bajo. Las colas añaden valor a partir de ~1000 jobs/hora o cuando la latencia de respuesta es crítica.

## [2026-06-07] Arquitectura de deploy cambiada de K8s+Docker a Render
**Problema:** El README §12 especifica K8s + Nginx + Docker para producción, lo cual requiere un servidor VPS propio, nginx, certbot y mantenimiento de infraestructura.
**Causa:** El usuario necesita deploys simples vía git push sin administrar infraestructura.
**Solución:** Se adopta Render (PaaS) para el API (Web Service) y el frontend (Static Site). Render maneja TLS, health checks, rolling deploys y scaling. Los archivos Docker y nginx.conf se mantienen en el repo como fallback para deploy manual si se necesita. El deploy es: push a main → GitHub Actions CI → Render Deploy Hook → `prisma migrate deploy` automático.
**Lección:** Para startups y MVPs, PaaS (Render, Railway, Fly.io) > K8s propio. K8s tiene sentido cuando el costo del PaaS supera el costo del equipo de infraestructura.

## [2026-06-07] BullMQ + Upstash Free: referencia cruzada
**Nota:** La decisión completa y la implementación están documentadas en la entrada "BullMQ reemplazado por ejecución síncrona para MVP" más abajo. En render.yaml y deploy.yml la variable `BULLMQ_REDIS_URL` está ausente intencionalmente.

## [2026-06-07] GET /api/v1/admin/users no implementado
**Problema:** El README §5.5 lista GET /api/v1/admin/users [ADMIN] pero GET /api/v1/users ya implementado en Fase 3 sirve exactamente el mismo propósito con los mismos controles de acceso (middleware authenticate + authorize ADMIN).
**Causa:** La especificación duplicó el endpoint por convención de prefijo /admin.
**Solución:** No duplicar. El frontend admin usará GET /api/v1/users directamente.
**Lección:** Revisar solapamiento de endpoints antes de implementar — si la misma data está disponible en otra ruta protegida, no crear alias innecesarios.

## [2026-06-09] Estado REAL del deploy (corrige entradas previas sobre migraciones y DB)
**Problema:** Entradas anteriores (y `napkin.md`) dicen que el deploy corre `prisma migrate deploy` automático en el buildCommand y que `DATABASE_URL` usa el Transaction pooler (puerto 6543). Ninguna de las dos refleja el estado real ya deployado y verificado.
**Causa:** El proyecto nunca generó archivos de migración, y el Transaction pooler (pgbouncer, 6543) no soporta DDL (`db push`/migrate) porque el schema no define `directUrl`.
**Solución (estado actual verificado en producción):**
- **Sin archivos de migración.** El esquema se aplica con `npx prisma db push` corrido **manualmente** por el usuario. El buildCommand de Render **NO** corre migrate.
- **`DATABASE_URL` = Session pooler, puerto 5432** (`...pooler.supabase.com:5432/postgres`, sin `pgbouncer=true`). Sirve para runtime y para `db push`.
- **Build command real de la API:** `pnpm install --frozen-lockfile --prod=false && pnpm --filter @bract/shared build && pnpm --filter @bract/api build`. Se quitó `corepack enable` (rompía con EROFS: pnpm ya viene en la imagen). `--prod=false` instala devDeps (typescript/prisma) necesarias para buildear.
- **`NODE_VERSION=20`** seteado por env var (sin pin, Render agarraba Node 26).
- **`packages/shared`**: exports condicionales (`node`→`dist`, `default`→`src`). **`apps/web/tsconfig.json`**: `declaration:false`. `packages/shared/tsconfig.json`: `ignoreDeprecations:"5.0"`.
- **`PRISMA_SKIP_POSTINSTALL_GENERATE=true`** en Render para silenciar el warning de schema en el postinstall.
- Static site (web) con regla de **rewrite `/*` → `/index.html`** (SPA).
**Lección:** Para modelos NUEVOS, el flujo es: editar `schema.prisma` → el usuario corre `npx prisma db push` con la URL 5432 → deploy normal. No asumir migraciones automáticas.

## [2026-06-09] Agente A — Contrato de datos de producto (tipos + Zod en @bract/shared)
**Problema:** Las 3 features de estudio (planner/flashcards/chat) necesitan un contrato de datos compartido (entidades + DTOs de I/O) consumido por API y web, antes de que B/C/D/E implementen.
**Causa:** A.2/A.3 del PLAN_AGENTES: tipos en `packages/shared/src/types/` y Zod en `schemas/`, exportados desde `@bract/shared`.
**Solución:** 4 pares type+schema alineados a C/D/E (`subject`, `study`, `flashcard`, `chat`) + reexports en `index.ts`. Decisiones tomadas:
- **Fechas en entidades de respuesta = `string` (ISO)**, no `Date`. Es el contrato JSON que cruza el cable (patrón de `notification`/`admin`). El mapeo `Date→string` (vía `toISOString`) vive en el service/controller de C/D/E, consistente con la fundación.
- **Tipos de DTO de input = `z.infer<typeof schema>`** exportados desde el `.schema.ts` (sin duplicar interfaces → sin drift). Las entidades de salida sí son `interface` en `.types.ts`.
- **`Subject.color` = paleta cerrada `z.enum(SUBJECT_COLORS)`** — 8 hex curados armonizados con los tokens del design system (§9.2 dark-first): brand indigo + estados info/success/warning/error + 3 acentos (purple/pink/teal). Exportada para reuso del frontend (swatches del selector). Descartado regex hex libre por la regla §3.3 "paleta permitida".
- **Tablas-hijo sin DTO de `userId`:** `StudyPlanItem`/`ChatMessage` se scopean por su padre (§3.4); sus schemas validan solo `planId`/`sessionId` y el body del bloque/mensaje.
- **Contratos de salida de la IA NO van acá:** los define y valida el Agente B (Apéndice C). A entrega solo entidades + DTOs de request.
- **Enums espejados de Prisma como `export enum` en `.types.ts`** (patrón de `Role`/`NotificationType`): los 6 enums de producto (`TopicStatus`, `TopicDifficulty`, `StudyPlanStatus`, `StudyPlanItemStatus`, `FlashcardSource`, `ChatRole`) replican exactamente los valores del `schema.prisma`. Zod los consume con `z.nativeEnum(...)` → una sola lista de valores, sin divergencia entre DB, validación y tipos.
- **Paginación selectiva en listados:** se paginan los de crecimiento no acotado (`chat sessions`, `flashcards?topicId`, `flashcards/due` con `limit`); `subjects`/`topics` NO se paginan porque el planner los consume como árbol completo (`SubjectWithTopics`) y paginarlos rompería el render día-por-día.
**Lección:** El contrato compartido se entrega como *entidades de salida* (interfaces, fechas ISO) + *DTOs de input* (z.infer). Mantener una sola fuente de verdad por DTO (el schema Zod) y por enum (Prisma → `z.nativeEnum`) evita que tipo y validación se desincronicen. Tras tocar `packages/shared`, rebuildear su `dist` (export condicional `node`→`dist`) para no dejar el runtime desfasado para el agente siguiente.

## [2026-06-09] `pnpm -r lint` roto a nivel fundación — eslint nunca instalado (TAREA AGENTE G)
**Problema:** `pnpm -r lint` falla con `"eslint" no se reconoce como un comando`. Los scripts `lint` de `apps/api` (`eslint src --ext .ts`) y `apps/web` (`eslint src --ext .ts,.tsx`) referencian un binario inexistente.
**Causa:** eslint nunca se instaló ni configuró en el repo: **0 ocurrencias en `pnpm-lock.yaml`**, no figura en ningún `package.json` (solo `typescript` como devDep raíz), y no hay `.eslintrc*` en ninguna parte. Es un script muerto del scaffolding de la fundación, preexistente al Agente A.
**Solución (PENDIENTE — Agente G, NO Agente A):** el Agente A NO lo arregla (excede su mandato de tipos+Zod y correría el linter sobre el código preexistente de la fundación). Para el Agente G:
1. Instalar + configurar eslint en el monorepo (config compartida + `@typescript-eslint`, reglas del CLAUDE.md: no `console.log`, no `any` sin `// DECISIÓN`).
2. Arreglar el step de lint del CI (hoy correría contra un binario inexistente).
3. Revisar y arreglar los hallazgos sobre el código existente de `apps/api` y `apps/web`.
**Mitigación del Agente A:** los archivos nuevos de `packages/shared` se verificaron a mano (grep): **cero `console.log`, cero `any`**; `pnpm -r typecheck` quedó en verde. El commit del entregable A se hizo con typecheck verde y verificación manual, con el lint documentado acá como deuda de G.
**Lección:** No declarar "lint verde" como criterio de hecho sin verificar que el linter exista y corra. Un script en `package.json` no implica que la herramienta esté instalada.

## [2026-06-10] Agente B — Núcleo de IA (capa `lib/ai` reutilizable)
**Problema:** Las 3 features de estudio (planner/flashcards/chat) necesitan una capa de IA compartida con el proveedor detrás de una env var, ensamblador de contexto del estudiante y funciones tipadas, validando SIEMPRE la salida con Zod y degradando si falta la key (sin romper build).
**Causa:** Agente B del PLAN_AGENTES (Fase 9 + Apéndice C). Depende de A (tipos de `@bract/shared`).
**Solución:** Módulo `apps/api/src/lib/ai/` (`ai.client` / `ai.schemas` / `ai.context` / `ai.prompts` / `ai.service` / `index`) + tests con mock. Decisiones tomadas:
- **Proveedor: Anthropic Claude** vía `@anthropic-ai/sdk` (única librería nueva fuera del stack del README §1 — documentada acá). **Modelos escalonados por tarea, NO Opus para todo:** `AI_MODEL_GENERATION=claude-haiku-4-5` (plan + flashcards, barato) y `AI_MODEL_CHAT=claude-sonnet-4-6` (chat; opus opcional). Strings tomados de los docs actuales del proveedor, no inventados.
- **`AI_API_KEY` OPCIONAL en `env.ts`** (`.optional()`, sin `process.exit`). La app **bootea sin ella** y degrada: `generateStudyPlan` devuelve una **distribución base determinista** (urgencia por examen + pendientes + minutos/día) → el planner básico funciona sin IA; `generateFlashcards`/`chat` lanzan `AppError('AI_UNAVAILABLE')`. Mismo fallback ante fallo del proveedor en el plan (nunca lanza).
- **Nuevo código de error `AI_UNAVAILABLE` (503)** agregado a `config/constants.ts` (`ERROR_CODES`), `lib/errors.ts` (`STATUS_MAP`) y README §5.3. Desviación: el set original era cerrado; "IA no disponible" no encajaba en `INTERNAL_ERROR` genérico.
- **`effort`/`adaptive thinking` NO se mandan en Haiku** (devuelven 400). Generación usa solo structured outputs; `effort: 'medium'` se manda **solo en chat** con guard `isEffortCapable(model)` (allowlist opus-4.x/sonnet-4-6/fable-5) para tolerar overrides de env.
- **Structured outputs por JSON Schema a mano** (`output_config.format`), NO el helper `zodOutputFormat` del SDK: el SDK 0.104.1 tipa ese helper contra **zod v4** y el repo usa **zod v3** → mismatch de tipos (`parsed_output` quedaba `{}`). Se pasa el JSON Schema manual y se valida el texto crudo con los Zod de `ai.schemas.ts` (`safeParse`) → no se confía en el structured output a ciegas. Invariantes en código: todo `topicId` existe, `Σ minutos/día` clampeado a la disponibilidad, flashcards capadas (≤10) y deduplicadas.
- **B no toca Prisma ni HTTP:** recibe DTOs, devuelve datos tipados. Persistencia (`ChatMessage`), SSE del chat y rate-limit por ruta son de C/D/E. El chat se diseñó como `AsyncGenerator<string>` (streaming) — excepción documentada al envelope JSON.
- **Contexto del chat ACOTADO** (`ai.context.ts`): resume materias/temas/progreso, lista hasta 15 pendientes por materia con "(+N más)" — no vuelca toda la DB (tokens/costo).
- **Tooling de tests:** se agregó **`vitest` (devDependency, test-only, no va a prod)** porque el repo no tenía runner. Tests excluidos de `tsconfig` (build limpio) y de typecheck; corren con `vitest run`. Setup `vitest.setup.ts` puebla env dummy válido para evitar el `process.exit(1)` de `env.ts` al importar. Se fijó `vitest@^2` (vite 5; vitest 4 pide vite 6+ y rompía peers con `apps/web`).
- **Dependencias:** `pnpm install` corrido y **`pnpm-lock.yaml` commiteado** — CI/Render usan `--frozen-lockfile` y se rompen si el lock no refleja las deps nuevas.
**Lección:** Cuando el helper tipado de un SDK apunta a una versión mayor de una dep (zod v4) distinta a la del repo (v3), no forzar el helper: pasar el JSON Schema a mano y validar con la versión local de Zod desacopla y cumple "validar siempre". La salida de la IA se trata como no confiable: schema + invariantes de negocio en código, y degradación con error manejado (nunca romper la app).

## [2026-06-10] Agente C — Planificador (backend: capas + recálculo determinista)
**Problema:** El planner necesita CRUD de materias/temas/disponibilidad + generación y recálculo del cronograma, reusando la distribución del Agente B y degradando sin `AI_API_KEY`, sin duplicar el algoritmo ni romper las capas.
**Causa:** Agente C del PLAN_AGENTES (Fase 10). Depende de A (tipos `@bract/shared`) y B (`lib/ai`).
**Solución:** Módulo `apps/api/src/modules/planner/` (`subject.repository` · `topic.repository` · `study.repository` → `planner.service` → `planner.controller` → `planner.routes`, montado en `/api/v1`). Decisiones tomadas:
- **Dos niveles de recálculo (decisión de producto, confirmada por el usuario):** la **IA solo se usa en la generación explícita** `POST /study/plan/generate` (`generateStudyPlan` de B: determinista + refinamiento IA, degrada al baseline si falta la key o falla). El **recálculo incremental** —completar un tema (`PATCH /topics/:id/status`) y **saltar un bloque** (`PATCH /study/plan/items/:id` con `SKIPPED`)— usa la **distribución 100% determinista, sin IA** (`generateStudyPlanBaseline`): adapta el plan al instante, sin costo de tokens ni reshuffle caótico. Marcar un bloque `COMPLETED` solo lo registra (no recalcula).
- **Export aditivo en `lib/ai` (coordinación C↔B):** se expuso `generateStudyPlanBaseline(input)` desde `lib/ai/index.ts`, que reusa el `buildBaselinePlan` interno de B. C **no duplica** el algoritmo (fuente única de la distribución). Cambio mínimo (1 wrapper en `ai.service.ts` + 1 línea en `index.ts`); no altera el comportamiento existente de B.
- **Persistencia:** generación explícita = archivar el `StudyPlan` ACTIVE anterior (`ARCHIVED`) + crear uno nuevo. Recálculo incremental = mutar el **mismo** plan ACTIVE en transacción: borra solo los bloques **futuros PENDING** (`date >= hoy AND status = PENDING`) y recrea desde el baseline, **preservando el historial** (COMPLETED/SKIPPED y todo lo anterior a hoy). No crea plan nuevo ni archiva.
- **API plana, agrupado en el frontend:** `GET /study/plan` y el generate devuelven `StudyPlanWithItems` plano (items `orderBy [date, order]`); el agrupado día-por-día lo hace el frontend.
- **Endpoints que recalculan devuelven el plan junto al recurso:** `{ topic, plan }` y `{ item, plan }` (ahorra un round-trip; el frontend refleja el cronograma recalculado sin refetch extra).
- **Ownership de `StudyPlanItem` vía el plan padre (§3.4):** no tiene `userId` propio; `study.repository.findPlanItemWithOwner` trae `plan: { id, userId }` y el service compara contra `req.user.id` → `NOT_FOUND` si no pertenece.
- **Cast de enums Prisma↔shared:** mismo patrón que `notification.service` (Date→ISO + `as` para el enum compartido en los mappers). En los `data` de Prisma con `exactOptionalPropertyTypes`, castear al tipo de enum **puro** del modelo (`PrismaTopic['difficulty'|'status']`), **no** al field-type de `Prisma.TopicXxxInput['...']` (incluye `| undefined` y rompe la asignación). Ver napkin.
- **Sin env vars nuevas, sin modelos nuevos:** `AI_API_KEY` ya la introdujo B; los modelos los aplicó A con `db push`. C no requiere `db push`.
- **Tests:** `planner.service.test.ts` (7 casos, repos + `lib/ai` mockeados): mapeo de DTOs, generación con IA, recálculo determinista al completar/saltar, no-recálculo al completar bloque, plan=null sin plan activo, y ownership ajeno → `NOT_FOUND`. `pnpm -r typecheck` verde; `vitest run` 20/20.
**Lección:** Reservar la IA para la acción explícita y resolver las micro-interacciones con la distribución determinista mantiene el plan reactivo, barato y < 100ms. Reusar el algoritmo de B vía un export aditivo (en vez de duplicarlo en C) preserva la fuente única. El backend queda listo para que el Agente F conecte las invalidaciones cruzadas hacia chat/flashcards.

## [2026-06-10] Agente D — Flashcards + SRS (backend por capas + motor SM-2 + frontend)
**Problema:** Flashcards necesita CRUD manual + generación con IA por tema + un motor de repaso espaciado (SRS) con endpoint de calificación y cola "due", sin duplicar la IA del Agente B ni romper las capas, degradando si falta `AI_API_KEY`.
**Causa:** Agente D del PLAN_AGENTES (Fase 11, Apéndice B). Depende de A (tipos `@bract/shared`) y B (`lib/ai`).
**Solución:** Módulo `apps/api/src/modules/flashcards/` (`flashcard.repository` → `srs.ts` (puro) → `flashcard.service` → `flashcard.controller` → `flashcard.routes`, montado en `/api/v1`) + `features/flashcards/` en el frontend. Decisiones tomadas:
- **Coordinación D↔A — `FlashcardWithTopic` aditivo en `@bract/shared`:** `GET /flashcards/due` devuelve la carta + `{ topic: { id, name, subjectName } }` porque la sesión de estudio cruza varios temas/materias y muestra "Repasando: <tema> · <materia>". Extensión aditiva del contrato de A (mismo patrón que C reusando el baseline de B): se agregó `FlashcardWithTopic extends Flashcard` a `flashcard.types.ts` (los DTOs ya existían). Se resuelve con UNA query (`include topic→subject select name`), sin N+1.
- **Motor SRS aislado y puro (`srs.ts`):** `reviewSrs(state, quality, now)` sin DB/HTTP/reloj propio — es la lógica más crítica/testeable. SM-2 simplificado del Apéndice B. **DECISIÓN sobre la única ambigüedad del algoritmo:** en `reps>=3` el intervalo usa el **ease PREVIO** (`round(intervalDays * ease)`) y el ease se actualiza DESPUÉS — orden canónico de SuperMemo SM-2 y coincide con el orden del Apéndice B (intervalo, luego ease). `q<3` resetea `reps=0`, `intervalDays=1`, `ease=max(1.3, ease-0.2)`.
- **Sesgo de ease inicial por dificultad (constante con nombre, confirmada por el usuario):** `INITIAL_EASE_BY_DIFFICULTY = { EASY: 2.6, MEDIUM: 2.5, HARD: 2.3 }`. Se aplica al **crear** carta (manual y generada por IA): ease menor → intervalos más cortos → los temas difíciles aparecen más seguido (criterio de hecho). `dueDate` arranca en `now()` (default del modelo) → entra a la sesión enseguida.
- **Generación con IA = inherente a IA:** `POST /topics/:topicId/flashcards/generate` reusa `generateFlashcards` de B (dedup + cap≤10 ya en B; le paso `existing` = preguntas del tema). Si falta la key o falla → `AI_UNAVAILABLE (503)`; el **CRUD manual y el SRS NO dependen de esto** (siguen andando). Persistencia con `createManyReturning` = transacción de `create`s (Prisma `createMany` no retorna registros; lote acotado ≤10).
- **Ownership por `userId` denormalizado (§3.4):** todas las queries de carta scopean `where { id/topicId, userId }` sin join. Crear/generar valida que el **Topic** sea del usuario (`findTopicContext`). Cast de enum Prisma↔shared (`FlashcardSource`) en los mappers (Date→ISO), patrón notification.service / napkin.
- **Frontend `features/flashcards/`:** dos vistas (tabs Estudiar / Mis tarjetas) con los 4 estados e i18n es/en (cero strings hardcodeados; `nav.flashcards` + bloque `flashcards.*`, plurales `_one/_other` para `count`). La **StudySession** congela un **snapshot local** de la cola due (calificar manda la carta al futuro y la sacaría de `due`, lo que correría los índices) y avanza por índice; "Repasar de nuevo" re-snapshotea con refetch. La vista de gestión **reusa `useSubjects` del planner** (fuente única del árbol materias→temas, alineado con Agente F). Entrada en sidebar + ruta lazy `/flashcards`.
- **Sin env vars nuevas, sin modelos nuevos, sin deps nuevas:** `AI_API_KEY` la introdujo B; el modelo `Flashcard` lo aplicó A con `db push`; vitest ya estaba. D no requiere `db push` ni toca `pnpm-lock.yaml`.
- **Tests:** `srs.test.ts` (11 casos): fallo resetea reps/intervalo y baja ease 0.2; piso 1.3; progresión 1d/6d/`round(int*easeViejo)`; round (no floor); fórmula de ease q=3/4/5; sesgo inicial por dificultad. `pnpm -r typecheck` verde; `vitest run` 35/35.
**Lección:** Aislar el SRS como función pura (sin DB ni reloj) lo hace trivialmente testeable y blinda la lógica más crítica del feature. La cola de estudio se debe congelar en cliente: si se lee directo del query `due`, calificar la refetchea y corre los índices a mitad de sesión. Reusar el árbol del planner (un solo `useSubjects`) en vez de duplicar la fuente de verdad deja el terreno listo para que F conecte las invalidaciones cruzadas.

## [2026-06-11] Agente E — Chat de estudio (backend por capas + streaming SSE)
**Problema:** El chat necesita CRUD de sesiones + un endpoint de mensaje que ensamble el contexto del estudiante (vía B), llame al proveedor con **streaming** y persista el hilo, sin romper las capas ni el envelope JSON, validando pertenencia de `ChatMessage` (que NO tiene `userId`) y degradando sin `AI_API_KEY`.
**Causa:** Agente E del PLAN_AGENTES (Fase 12, Apéndice C). Depende de A (tipos `@bract/shared`) y B (`lib/ai`: `streamChatReply`, `assembleStudentContext`).
**Solución:** Módulo `apps/api/src/modules/chat/` (`chat.repository` → `chat.service` → `chat.controller` → `chat.routes`, montado en `/api/v1`). Decisiones tomadas:
- **DESVIACIÓN — el endpoint de mensaje NO usa el envelope `{ success, data }`:** `POST /chat/sessions/:id/messages` responde **Server-Sent Events** (`text/event-stream`). El streaming token-a-token es incompatible con un body JSON único; es la excepción documentada (ya anticipada por B, que diseñó `chatReply` como `AsyncGenerator`). Los otros 4 endpoints (listar/crear/obtener/borrar sesión) sí usan el envelope normal.
- **Formato SSE exacto** (eventos nombrados + `data` JSON; `data` JSON-encodeado para que newlines/Unicode queden seguros en una sola línea):
  ```
  event: meta\ndata: {"sessionId":"...","userMessageId":"...","title":"..."}\n\n
  event: token\ndata: {"text":"<delta>"}\n\n      (uno por cada delta del proveedor)
  event: done\ndata: {"messageId":"<assistantId>"}\n\n
  event: error\ndata: {"code":"AI_UNAVAILABLE","message":"..."}\n\n   (si falla ya streameando)
  ```
  Headers: `Content-Type: text/event-stream`, `Cache-Control: no-cache, no-transform`, `Connection: keep-alive`, `X-Accel-Buffering: no` (mata el buffering de proxy en Render/Nginx → tokens en tiempo real).
- **Separación de capas para el stream:** el **service** expone `streamMessage()` como `AsyncGenerator<ChatStreamEvent>` (HTTP-agnóstico: emite `{type:'meta'|'token'|'done'}`); el **controller** es la ÚNICA capa que toca `res` (serializa cada evento a un frame SSE). El service nunca recibe `req`/`res`.
- **Errores ANTES vs DESPUÉS del primer frame:** el generador valida ownership y `isAIConfigured()` ANTES del primer `yield` (y antes de persistir el mensaje del usuario). Como un `AsyncGenerator` no corre su cuerpo hasta el primer `.next()`, esos throws caen en el `for await` del controller con `started=false` → `next(err)` → **JSON** (`NOT_FOUND` 404 / `AI_UNAVAILABLE` 503). Una vez enviado el `meta` (headers 200), un fallo del proveedor ya no puede cambiar el status → sale como `event: error`.
- **Persistencia del reply (lo más frágil — el guardado al final):** el generador acumula los deltas en `acc` y persiste el `ChatMessage` del assistant en un bloque **`finally`**, no en el happy-path. Así un mismo bloque cubre los dos finales: (a) **stream completo** → `streamEnded=true`, el `finally` crea el mensaje con todo `acc` y toca `updatedAt`, y RECIÉN DESPUÉS se emite `done` con el `messageId` real; (b) **disconnect a mitad** → el controller llama `gen.return()` en `res.on('close')`, lo que corre el `finally` con el `acc` parcial (>0) y lo persiste igual (registrado, no a medias-perdido), sin emitir `done`. Guardas: persiste solo si `acc.length>0`; el `try` alrededor del `createMessage` logguea con Winston si la DB falla (no tira el proceso).
- **Abort real e INMEDIATO del proveedor en disconnect (AbortController atado al close):** el controller crea un `AbortController` y llama `aiAbort.abort()` en `res.on('close')`. El `signal` baja por `chatService.streamMessage(..., signal)` → `streamChatReply(input, signal)` (cambio **aditivo y retrocompatible** en B — coordinación E↔B, mismo patrón que C exponiendo `generateStudyPlanBaseline`) → `client.messages.stream(params, { signal })`, que el SDK 0.104.1 ata a su `AbortController` interno y aborta el fetch al instante. El service trata el throw por abort como corte ESPERADO (`if (!signal?.aborted) throw err`): no relanza (el controller NO emite `event: error`), y el `finally` persiste el parcial. **Por qué no alcanzaba solo `gen.return()`:** verifiqué el SDK (`lib/MessageStream.js`: el `[Symbol.asyncIterator]().return()` llama `this.abort()`), así que cortar el `for await` SÍ aborta — pero `.return()` se encola detrás del `.next()` en vuelo, por lo que el abort recién se inyecta en el **próximo límite de token** (podía generar ~1 token más). Con el `AbortController` el corte es inmediato (0 tokens extra).
- **Ownership de `ChatMessage` vía el padre (§3.4):** `ChatMessage` no tiene `userId`. `chat.repository.findOwner(sessionId)` trae `{ id, userId, title }` y el service compara contra `req.user.id` → `NOT_FOUND` si no pertenece (mismo patrón que C con `StudyPlanItem` vía `plan.userId`). Borrar/obtener sesión scopean por `{ id, userId }`.
- **Contexto del estudiante REUSANDO el árbol del planner:** el service llama `plannerService.listSubjects(userId)` (→ `SubjectWithTopics[]`, fuente única materias→temas, alineado con F) y lo pasa a `assembleStudentContext` de B. El acotado por tokens (resumen, ≤15 temas/materia, próximo examen) ya vive en B; E no lo duplica.
- **Historial acotado:** se mandan al modelo los últimos `MAX_HISTORY_MESSAGES=20` turnos user/assistant (constante, configurable), traídos newest-first y revertidos a cronológico, excluyendo el mensaje actual y los `SYSTEM`. El contexto del estudiante va aparte (no cuenta contra ese cap).
- **`title` derivado del 1er mensaje:** si `session.title===null` al primer mensaje, se setea con el `content` (espacios colapsados, truncado a 60 con elipsis) y viaja en el `meta` para que el front actualice la lista al vuelo. Editable después no está en alcance de E.
- **Sin env vars nuevas, sin modelos nuevos, sin deps nuevas:** `AI_API_KEY` la introdujo B; `ChatSession`/`ChatMessage` los aplicó A con `db push`. E NO requiere `db push`. `pnpm -r typecheck` verde (api + web).
**Lección:** Para streaming sobre Express, mantené la separación de capas haciendo que el service sea un `AsyncGenerator` de eventos de dominio y que el controller sea el único que escribe en `res`. Persistí el reply en un `finally` (no en el happy-path): un solo lugar cubre completar y desconectar, y nunca perdés lo ya generado. Chequeá disponibilidad de IA ANTES del primer frame para poder devolver un 503 limpio; después del primer byte el status ya está sellado y los errores van como `event: error`.

## [2026-06-11] Agente F — Integración y contexto compartido (efecto SRS cruzado + invalidaciones)
**Problema:** Las 3 secciones (planner/flashcards/chat) debían compartir datos y contexto EN VIVO: completar/cambiar el estado de un tema en el planner tiene que reflejarse, sin recargar a mano, en (a) el recálculo del plan, (b) la frecuencia SRS de las flashcards de ese tema, y (c) el contexto que ve el chat; las flashcards solo sobre temas del planner; una sola fuente de verdad para materias/temas/progreso.
**Causa:** Agente F del PLAN_AGENTES (Fase 12). Depende de C/D/E (ya mergeados). El usuario fijó dos requisitos: (1) coupling limpio — el planner NO toca tablas de flashcards; (2) semántica explícita y testeada del efecto de "completar tema" sobre el SRS.
**Solución:** Cero estado duplicado a nivel de cliente y un efecto de dominio cruzado en el backend. Decisiones tomadas:
- **Fuente de verdad única — ya estaba bien montada, no se duplicó:** el árbol materias→temas vive en una sola query (`queryKeys.planner.subjects`); flashcards la consume con el MISMO `useSubjects` del planner (cache compartida) y el chat la arma server-side con `plannerService.listSubjects`. F NO introdujo una segunda copia.
- **El chat NO necesita invalidación de cliente:** ensambla su contexto en el backend en cada mensaje, así que el próximo mensaje ya ve el progreso real (temas pendientes/completados, próximo examen). "El chat referencia el progreso real" queda satisfecho por construcción (decisión de E reusando `plannerService.listSubjects`), sin nada que sincronizar en el front.
- **REGLA DELIBERADA del efecto SRS (requisito 2 del usuario, Opción B):** un tema en rotación de estudio (`IN_PROGRESS`/`COMPLETED`) tiene sus flashcards **ACTIVAS** en el SRS (entran al `due`) para reforzar la retención; un tema `PENDING` las **PAUSA** (salen del `due`). El efecto **solo mueve `dueDate`**; **preserva `ease`/`intervalDays`/`reps`** (no pisa lo aprendido por repasos reales). Es idempotente. La pausa fija `dueDate` a un centinela inalcanzable (`SRS_PAUSED_DUE_DATE` = año 9999, en `srs.ts`); "pausada" se detecta con `dueDate ≥ SRS_PAUSED_THRESHOLD` (año 9000, margen amplio sobre cualquier intervalo SM-2 real). **Activar reincorpora SOLO las cartas pausadas** (centinela → `now`), dejando intacto el schedule de las que ya estaban activas (`updateMany` filtra por el umbral). DECISIÓN del centinela vs columna nueva: una columna `srsPausedAt` sería más explícita pero exige `db push` (cambio de schema) y tocar la query `due` de D; el centinela mantiene el efecto autocontenido en la app, sin migración. Trade-off documentado; si en v2 se quiere robustez total, migrar a columna.
- **Coupling limpio (requisito 1 del usuario):** el efecto vive en `flashcardService.onTopicStatusChanged(userId, topicId, status)` — el flashcard module es dueño de su data. `plannerService.updateTopicStatus` **delega** ahí (un `await`), nunca toca tablas de flashcards. Dirección de import unidireccional `planner → flashcard` (sin ciclo: `flashcard.service` no importa planner; `chat.service` ya importaba planner). El repo de flashcards expone `pauseSrsByTopic`/`activateSrsByTopic` (solo Prisma `updateMany`, scope por `userId` denormalizado §3.4).
- **Invalidaciones cruzadas centralizadas (front):** `apps/web/src/lib/invalidateStudyContext.ts` concentra el grafo en un solo lugar: `invalidateAfterTopicStatusChange(qc, topicId)` (plan + árbol + `flashcards.byTopic` + `flashcards.due`) e `invalidateAfterTreeChange(qc)` (plan + árbol + `flashcards.all()`, porque borrar materia/tema cascadea a flashcards). `useTopicMutations` y `useSubjectMutations` consumen esos helpers en vez de alcanzar keys de flashcards ad hoc.
- **Boundary explícito:** el efecto reacciona a la TRANSICIÓN de `Topic.status`; la creación de cartas (Agente D) sigue naciendo activa (`dueDate=now`), no se tocó. Pausar/activar es la reacción deliberada al cambio de estado, no un invariante recomputado en cada create.
- **Sin env vars nuevas, sin modelos nuevos, sin deps nuevas, sin `db push`** (el efecto solo escribe `dueDate` en filas existentes). `pnpm -r typecheck` verde (shared+api+web); `vitest run` 39/39 (4 tests nuevos de `onTopicStatusChanged` + 1 aserción de delegación en `planner.service.test`).
**Lección:** El "contexto compartido en vivo" se logra con UNA fuente de verdad (no copiar el árbol) + efectos de dominio cruzados detrás del service dueño de cada data (coupling limpio: el planner delega, no alcanza tablas ajenas) + un grafo de invalidaciones React Query centralizado en un módulo. Lo que se lee server-side por request (contexto del chat) no necesita invalidación de cliente. Un efecto reversible que solo mueve `dueDate` (sin tocar ease/interval/reps) implementa "frecuencia SRS" sin migración ni riesgo de pisar el aprendizaje del usuario.

## [2026-06-12] Agente G — Polish, i18n, bugs + fundación de ESLint
**Problema:** La fundación necesitaba pulido (README §8): 4 bugs funcionales, i18n incompleto (toda la auth hardcodeada + `titleKey` sin resolver), ESLint nunca instalado (scripts `eslint` rotos → CI rojo), error boundaries y colores hardcodeados. Sin tocar backend ni el Textarea.
**Causa:** Agente G del PLAN_AGENTES (Fase 8). Independiente de A–F (paralelizable). El usuario fijó: "Analítica" en español (no "Analytics"); los `any` justificados llevan `eslint-disable-next-line` + `// DECISIÓN`; lint VERDE sin volverlo infinito.
**Solución:** Cuatro fases, todo en el front (`apps/web`) + config de tooling en la raíz.
- **Bugs funcionales (Fase 1):** (a) Perfil "Invalid Date" + nombre vacío = el front leía `data` plano pero el backend envuelve en `data.user` (`profile.controller`) y `data.user` en `/auth/me` — se corrigió el unwrap en `profile.api.ts` y `auth.api.ts`. (b) Notificaciones con lista vacía pese al contador = el backend devuelve `data.notifications`, no `data.items` (`notification.controller`) — corregido en `notifications.api.ts` + `useNotifications`. (c) Toggle de idioma ES/EN en el `Header` (`i18n.changeLanguage`), persistido por sesión. (d) Fechas localizadas (`timeAgo.ts` + `ProfileCard.formatDate`) según `i18n.language`. NINGÚN cambio de backend (solo se alineó el contrato de lectura del front al envelope real).
- **i18n (Fase 2):** Bloque `auth.*` completo es/en y **cableado de TODAS las pantallas de auth** (Login/Register/Forgot/Reset/VerifyEmail Form+Notice+Page) que estaban 100% hardcodeadas — era acá donde había cortado la sesión previa (las keys ya estaban en `i18n.ts`, faltaba consumirlas). `getApiErrorMessage(error, fallback)` se refactorizó para recibir el fallback i18n (la función es pura, fuera del componente, sin acceso a `t`). El email en bold del `VerifyEmailNotice` usa `<Trans components={{ b: <span/> }}>`. `titleKey`/`labelKey` de las rutas se resuelven con `t()` en `DashboardShell`/`Header`. "Analítica" en el nav y títulos (decisión del usuario). Grep de strings sueltos en C/D/E/F → limpio (esos features ya usaban `t()`).
- **ESLint (Fase 3):** **DECISIÓN — ESLint 8 + `.eslintrc.cjs` (no flat config)** para mantener los scripts `eslint src --ext` existentes (flat config rompe `--ext`) y typescript-eslint **v7** (compatible con TS 5.4; v8 pide ≥5.5). **Sin `parserOptions.project`**: `no-console` y `@typescript-eslint/no-explicit-any` (las reglas duras del proyecto, a ERROR) no necesitan type-info → lint rápido y sin fricción de tsconfig. Ruido relajado a `warn`/`off` (`no-unused-vars` warn con `^_`, `no-non-null-assertion`/`ban-ts-comment`/`no-empty-function` off) para que sea verde y útil, no infinito. Config única en la raíz (`root:true`) con `overrides` por path (web→browser+react-hooks, api/packages→node). Deps instaladas en la raíz con `pnpm add -w -D` (los scripts anidados resuelven el binario vía el `.bin` de los ancestros). **El step de CI no se tocó** — ya tenía "Lint API/Web"; solo fallaba porque ESLint no existía: instalarlo + commitear el lockfile lo deja verde con `--frozen-lockfile`. Resultado: **0 errores, 0 warnings** en api + web (el codebase de A–F ya venía sin `any` ni `console.log`; los 2 warnings residuales se limpiaron: var muerta en `DashboardShell`, y un `eslint-disable` con `// DECISIÓN` en el debounce de `UsersFilters` que a propósito no depende de `onFiltersChange`).
- **Polish (Fase 4):** El `ErrorBoundary` por ruta ya existía (envuelve cada ruta lazy en `router/index.tsx`) y delega en `ErrorState`; se i18n-izaron sus defaults hardcodeados (`title`→`common.error`, botón→`common.retry`) — ahora el fallback usa tokens + i18n. Grep de colores hardcodeados → solo 2, ambos pre-existentes y dinámicos: `subject.color ?? '#6366f1'` (dato de usuario) → fallback al token `var(--brand-primary)`; el `boxShadow` rgba inline del `NotificationDropdown` → clase Tailwind `shadow-2xl shadow-black/40` (consistente con el resto).
- **Sin backend, sin modelos, sin `db push`, sin tocar el Textarea.** Deps nuevas (solo tooling, devDeps de raíz): `eslint@8`, `@typescript-eslint/{parser,eslint-plugin}@7`, `eslint-plugin-react-hooks@4`; `pnpm-lock.yaml` actualizado y commiteado. `pnpm -r typecheck` + `pnpm -r lint` VERDES.
**Lección:** Varios "bugs de UI" eran en realidad desajustes del contrato de lectura del front contra el envelope real del backend (`data.user`, `data.notifications`) — antes de tocar el front conviene mirar qué forma exacta devuelve el controller. Para introducir ESLint tarde en un monorepo sin volverlo infinito: ESLint 8 + eslintrc (respeta los scripts `--ext` existentes), versión de typescript-eslint atada a la de TS, sin `parserOptions.project` (solo las reglas que importan, que no piden type-info), config única en la raíz con overrides por path, y deps en la raíz (los scripts anidados resuelven el binario). El step de CI ya estaba bien: "arreglar el lint del CI" era simplemente hacer que ESLint exista + commitear el lockfile.

## [2026-06-12] Swap del proveedor de IA: Anthropic Claude → Google Gemini (free tier)
**Problema:** Sin presupuesto para Anthropic. Gemini tiene free tier sin tarjeta. Hay que reemplazar el SDK manteniendo INTACTO el contrato público de `lib/ai` (C/D/E lo consumen): firmas de `generateStudyPlan`, `generateFlashcards`, `streamChatReply`, `chatReply`, `assembleStudentContext` y `generateStudyPlanBaseline` no cambian — solo la implementación interna (cliente + llamadas).
**Causa:** Decisión de costo. Tarea en rama propia `agente-gemini` (main intacto hasta el merge).
**Solución:** Cambio acotado a `apps/api/src/lib/ai/*` + `config/env.ts` + deps. Decisiones tomadas:
- **SDK: `@google/genai` (NO `@google/generative-ai`).** El usuario había nombrado `@google/generative-ai`, pero está **deprecado/EOL desde 30-nov-2025** (fixes terminaron 31-ago-2025) y no expone features nuevas. Se confirmó con el usuario y se usó el **SDK unificado oficial `@google/genai`** (GA estable desde may-2025): trae `abortSignal`, JSON mode (`responseMimeType`+`responseSchema`) y `generateContentStream` nativos. `pnpm remove @anthropic-ai/sdk` + `add @google/genai@^2.8.0`; `pnpm install` corrido y **`pnpm-lock.yaml` commiteado** (CI/Render usan `--frozen-lockfile`).
- **Modelos free tier (verificados en docs, NO inventados):** `AI_MODEL_GENERATION=gemini-2.5-flash-lite` (plan+flashcards, el más barato/rápido) y `AI_MODEL_CHAT=gemini-2.5-flash` (chat, más capaz). Ambos en free tier sin tarjeta, con JSON mode + streaming. Desde **abril-2026 los Pro salieron del free tier** (solo Flash/Flash-Lite) → `2.5-pro` descartado para no romper "sin presupuesto". Strings overrideables por env (existe también la familia 3.x en free tier).
- **`AI_API_KEY` se MANTIENE** con el mismo nombre genérico (ahora es la key de Gemini) y sigue OPCIONAL → la app bootea sin ella y degrada igual (plan → baseline determinista; flashcards/chat → `AppError('AI_UNAVAILABLE')` 503). `generateStudyPlanBaseline` es provider-agnóstico, **NO se tocó**.
- **Generación JSON:** `output_config.format = {type:'json_schema'}` de Anthropic → `config.responseMimeType:'application/json'` + `config.responseSchema` de Gemini. El `responseSchema` es subset de OpenAPI 3.0 y **NO admite `additionalProperties`** (que sí usaba el JSON Schema de Anthropic): se reescribieron `planResponseSchema`/`flashcardsResponseSchema` con el enum `Type` (OBJECT/ARRAY/STRING/INTEGER) y sin `additionalProperties`. Los **Zod (`planOutputSchema`/`flashcardsOutputSchema`) NO se tocaron**: la salida cruda se sigue validando con `safeParse` + invariantes en código (clamp minutos/día, dedupe, cap≤10). No se confía en el structured output a ciegas.
- **Chat (streaming):** `client.messages.stream` → `await client.models.generateContentStream`; los deltas `content_block_delta/text_delta` → `chunk.text` por chunk. `system` → `systemInstruction`; `messages[{role:'assistant'}]` → `contents[{role:'model', parts:[{text}]}]`; `max_tokens` → `maxOutputTokens`. El **formato SSE del controller (event: meta/token/done/error), el frontend y la persistencia en `finally` NO cambian** — solo cambió la fuente del stream. El **abort en disconnect** (AbortController de E atado a `res.on('close')`) baja igual por `streamMessage(…, signal)` → `streamChatReply(input, signal)` → ahora `config.abortSignal` de Gemini (en vez de `{ signal }` de Anthropic). Nota: en Gemini `abortSignal` es *client-only* (corta el fetch/iteración local al instante — lo que el controller necesita para dejar de emitir —, aunque no cancele el cómputo ya iniciado server-side).
- **Quitado lo Anthropic-specific:** `isEffortCapable`/`EFFORT_CAPABLE` y el parámetro `effort: 'medium'` del chat (Gemini no los usa). `isEffortCapable` no lo consumían C/D/E (solo mocks de test), así que removerlo no rompe el contrato público; `index.ts` nunca lo exportó.
- **Tests:** se reescribió el mock del SDK en `ai.service.test.ts` (forma Gemini: `getAIClient().models.generateContent` devuelve `{ text }`; `generateContentStream` devuelve `Promise<AsyncGenerator>` de chunks `{ text }`). Se actualizaron los strings de `AI_MODELS` y se quitó `isEffortCapable` de los mocks de `planner.distribution.test.ts`. `pnpm -r typecheck` verde; `vitest run` **39/39**; `pnpm --filter @bract/api lint` 0 errores.
- **Sin modelos nuevos, sin `db push`, sin cambios de frontend ni de controllers.** Solo `lib/ai/{client,schemas,service}.ts`, `lib/ai/__tests__/ai.service.test.ts`, `config/env.ts`, `planner/__tests__/planner.distribution.test.ts`, `package.json`, `pnpm-lock.yaml`. `ai.prompts.ts`/`ai.context.ts`/`index.ts` intactos (provider-agnósticos).
**Lección:** Para cambiar de proveedor de LLM sin romper a los consumidores, aislá el SDK detrás de una capa con firmas estables (acá `lib/ai` ya estaba bien diseñada: `getAIClient`/`AI_MODELS`/funciones tipadas) y tocá solo la implementación interna. Verificá SIEMPRE en los docs actuales (a) que el SDK que te pidieron no esté deprecado —`@google/generative-ai` lo está— y (b) los strings de modelo y su estado de free tier (los Pro salieron del free tier en abr-2026). El `responseSchema` de Gemini es OpenAPI-subset (sin `additionalProperties`), distinto del JSON Schema de Anthropic; mantené la validación Zod como red de seguridad independiente del formato que pida cada proveedor.

## [2026-06-12] Agente K — Importación masiva de temas POR TEXTO (extract + commit con preview)
**Problema:** Cargar temas de a uno es tedioso. Se necesita pegar texto grande y que la IA extraiga los temas + dificultad, con un preview editable antes de escribir, sin que la IA decida borrados.
**Causa:** Feature "Agente K" de `IDEAS_POST_MVP.md` (alta prioridad post-MVP). Reusa A (modelos Subject/Topic), B (`lib/ai`/Gemini) y el patrón de capas de C. Alcance acotado: SOLO texto (archivos PDF/.pptx/.txt/.md son follow-up, NO se implementaron). Rama `agente-k-import-texto` (sin merge a main).
**Solución:** Módulo nuevo `apps/api/src/modules/import/` (`import.repository` → `import.service` → `import.controller` → `import.routes`, montado en `/api/v1`) + `features/import/` en el frontend + función `extractTopics` aditiva en `lib/ai`. Decisiones tomadas:
- **Flujo en 2 pasos con PREVIEW obligatorio:** `POST /import/topics/extract` (`{ text, subjectName? }` → la IA extrae temas + dificultad → preview, **NO escribe en DB**) y `POST /import/topics/commit` (`{ topics[], mode, subjectId|subjectName }` → persiste). Nada se crea sin que el usuario revise/edite el preview.
- **El borrado lo decide el MODE, NUNCA la IA (requisito clave de diseño):** `mode: 'ADD'` agrega sin borrar, deduplicando contra los temas existentes (normalizado: minúsculas + espacios colapsados); `mode: 'REPLACE'` borra los temas de la materia y reemplaza. `ImportMode` es un enum **de request** (toggle de UI), NO un enum de Prisma. La IA solo extrae y clasifica — no interpreta intención de borrado desde texto libre (riesgo de perder temas por una frase ambigua).
- **Materia destino:** el commit recibe EXACTAMENTE una de `subjectId` (existente, valida ownership → `NOT_FOUND` si ajena) | `subjectName` (nueva → se crea). Schema Zod con `.refine((subjectId===undefined) !== (subjectName===undefined))`. Para materia nueva, `mode` se fuerza a ADD en el frontend (replace no aplica sobre una materia recién creada).
- **`extractTopics` aditivo en `lib/ai` (coordinación K↔B, mismo patrón que C con `generateStudyPlanBaseline`):** nuevos `topicsOutputSchema`/`topicsResponseSchema` (Gemini `responseSchema` OpenAPI-subset, sin `additionalProperties`), prompt `EXTRACT_TOPICS_SYSTEM`, y la función pública. **Dificultad validada laxa:** la IA puede devolver `"medium"`/`"media"`/minúsculas → se normaliza a EASY/MEDIUM/HARD en código (desconocida → MEDIUM), nunca falla el parse por eso. Salida validada con Zod (`safeParse`) + **dedup + cap≤50 en código** (no se confía en el structured output a ciegas). Inherente a IA: lanza `AI_UNAVAILABLE` (503) sin `AI_API_KEY` — la app no degrada esta feature (sin IA no hay extracción).
- **Self-contained, NO toca el backend de otras features:** `import.repository` hace sus propias queries Prisma (crear materia, `createMany` temas, `deleteMany` por materia en replace dentro de una transacción). NO importa los repos del planner. El `userId` se denormaliza al crear los temas (§3.4); en REPLACE, el `onDelete: Cascade` de `Topic→Flashcard` limpia las flashcards de los temas borrados (comportamiento intencional del reemplazo). Cast de enum `TopicDifficulty` Prisma↔shared en el service (patrón napkin: castear a `PrismaTopic['difficulty']`).
- **Doble round-trip de dedup (read-then-write):** el service lee los nombres existentes (`findSubjectWithTopicNames`, solo `select name` — sin traer filas completas, sin N+1) para deduplicar en ADD, luego `applyImport` aplica el lote atómico. Race aceptable (app personal, baja concurrencia).
- **Frontend `features/import/`:** wizard de 2 pasos. Paso 1 (`ImportTextForm`, RHF + zodResolver): textarea + elegir materia existente (Select reusando `useSubjects` del planner, fuente única del árbol) o nueva. Paso 2 (`ImportPreview`): lista editable (nombre `Input` + dificultad `Select` reusando labels `planner.difficulty.*` + quitar) + toggle add/replace + confirmar. Los 4 estados (loading/empty/error/success), i18n es/en completo (bloque `import.*` + `nav.import`, plurales `_one/_other`), entrada en el sidebar, ruta lazy `/import`. El commit invalida el árbol vía `invalidateAfterTreeChange` (Agente F) → planner y flashcards reflejan los temas nuevos. `AI_UNAVAILABLE` se detecta por `error.code` del envelope (patrón de `TopicFlashcards`).
- **Sin env vars nuevas, sin modelos nuevos, sin `db push`, sin deps nuevas:** `AI_API_KEY` ya existe (B); reusa `Subject`/`Topic` (A); vitest ya estaba. K NO requiere `db push` ni toca `pnpm-lock.yaml`.
- **Tests:** `import.service.test.ts` (5 casos, repo + `lib/ai` mockeados): preview no persiste; ADD deduplica contra existentes + interno y no borra (`replace=false`); REPLACE borra (`replace=true`) sin dedup contra existentes; materia nueva crea + agrega; ownership ajeno → `NOT_FOUND`. `ai.service.test.ts` (+3 casos de `extractTopics`): sin key → `AI_UNAVAILABLE`, normaliza dificultad laxa + dedup, capa la cantidad. `pnpm -r typecheck` + `lint` + `build` verdes; `vitest run` 47/47.
**Lección:** Para una importación masiva con IA, separá EXTRACT (preview, sin DB) de COMMIT (persistencia) y dejá que un toggle de UI —no la IA— decida el borrado: así un texto ambiguo nunca destruye datos. Mantené el módulo self-contained (su propio repo, sin tocar el backend de otras features) y reusá la capa de IA detrás de una función aditiva con firma estable. La salida de la IA se trata como no confiable: schema Zod + normalización laxa de los campos enumerados (la dificultad) + dedup/cap en código, y degradación con `AI_UNAVAILABLE` cuando la feature es inherentemente de IA.

## [2026-06-12] Agente H — gate de CI completo + tono del chat (cero `**`, listas con `·`)
**Problema:** (1) El deploy a `main` podía salir con lint/build/tests rotos: `ci.yml` (lint+typecheck+build) es `on: pull_request`, y como el proyecto mergea directo a `main` (sin PR), nunca corría para esos commits; el único gate real antes del deploy (`deploy.yml` → job `ci`, `needs` del job `deploy`) corría **solo typecheck**. Ningún workflow corría los tests (vitest). (2) El chat (Gemini) seguía metiendo negritas `**` pese a la instrucción previa, que además prohibía listas por completo — el usuario quiere cero `**`/títulos markdown pero SÍ listas con viñeta `·` y espaciado estilo Canva al enumerar.
**Causa:** (1) La fundación dejó la CI "real" (lint/build) atada a PRs, incompatible con el flujo direct-to-main de este repo (ver FID / napkin); el gate de deploy se había armado mínimo (typecheck). (2) El system prompt previo dejaba un resquicio "excepcional" para las negritas que el modelo abusaba, y vetaba toda lista.
**Solución:** Rama `agente-h-qa`.
- **`.github/workflows/deploy.yml`:** al job `ci` (gate de `deploy` vía `needs: ci`, `on: push` a `main`) se le agregaron `Lint API`, `Lint Web`, `Test` (`pnpm -r test`) y `Build API`/`Build Web` (con las `VITE_*` mínimas). Orden: build shared → typecheck → lint → test → build. `typecheck API` corre `prisma generate` primero, así el cliente Prisma existe para tests/build en checkout limpio. `ci.yml` (PR) no se tocó.
- **`apps/api/src/lib/ai/ai.prompts.ts`** (`buildChatSystemPrompt`, sección TONO Y FORMATO): refuerzo "PROHIBIDO ABSOLUTO" de `**` (sin la excepción previa) y de títulos markdown; se PERMITEN listas solo al enumerar, con viñeta `·` (punto medio) por ítem y un renglón en blanco entre ítems (estilo Canva), nunca `-`/`*`/`1.`; fuera de enumeraciones, prosa con ejemplos integrados al hilo. La línea del tono comprensible/prosa (marcada como perfecta por el usuario) se conservó intacta. `PLAN_SYSTEM`/`FLASHCARDS_SYSTEM` no se tocaron (salida JSON estructurada, sin markdown).
- Sin env vars, modelos, `db push` ni deps nuevas. `pnpm -r typecheck` + `lint` + `build` verdes; `vitest run` 39/39 (el cambio de prompt es un string; ningún test asserta el texto del system prompt).
**Lección:** Un check verde en un commit de `main` NO implica que lint/build/tests pasaron si la CI completa es PR-only — verificá QUÉ workflow corre en push y endurecé el gate que realmente bloquea el deploy. Para frenar markdown indeseado de un LLM, una prohibición absoluta sin escape ("cero `**` SIEMPRE, sin excepción") funciona mejor que una con resquicio "excepcional", que el modelo termina usando de excusa.

## [2026-06-13] Agente K (follow-up) — Importación de temas desde ARCHIVOS (PDF/.pptx/.txt/.md)
**Problema:** Extender la importación masiva del Agente K (hoy solo POR TEXTO) para aceptar ARCHIVOS: el archivo se convierte a TEXTO y alimenta el pipeline `extract → preview → commit` YA EXISTENTE, sin reescribirlo. Solo PDFs de texto (escaneados/imagen = OCR, fuera de alcance), .pptx (texto de slides), .txt/.md triviales. Con tope de tamaño de archivo y de texto extraído (cost/safety), errores manejados, libs mantenidas/seguras. Rama `agente-k-files` (sin merge a main hasta revisión).
**Causa:** Feature de `IDEAS_POST_MVP.md` (Agente K, "importar archivos"), pendiente del MVP por texto. Reusa A (Subject/Topic), B (`lib/ai`/`extractTopics`/Gemini) y todo el módulo `import/` existente.
**Solución:** Aditivo y self-contained — el paso 2 (preview/commit) y `commitImport` quedaron INTACTOS; solo se sumó una entrada "archivo→texto" antes del pipeline. Decisiones tomadas:
- **Librerías de parsing (mantenidas, sin EOL, sin advisories abiertos), confirmadas por el usuario:**
  - **PDF: `unpdf@1.6.2`** — wrapper de **unjs** (ecosistema Nuxt/Nitro, activo) sobre el motor **pdf.js de Mozilla** pineado reciente; pure-JS, sin deps nativas (no `canvas`), serverless-safe (Render). El parsing real lo hace el engine auditado de Mozilla. Solo extrae la capa de texto → un PDF escaneado devuelve vacío y se reporta como error manejado (no se intenta OCR).
  - **PPTX: `fflate@0.8.3` + `fast-xml-parser@5.7.3`** — un .pptx es un ZIP de XML; `fflate` (unzip pure-JS, ultra-mantenido) + `fast-xml-parser` para leer `ppt/slides/slide*.xml` y juntar los runs `<a:t>`, **una línea por párrafo `<a:p>`** (preserva bullets ≈ temas), slides en orden numérico. ~30 líneas propias en vez de una lib pptx pesada/dudosa.
  - **`.txt`/`.md`:** decode UTF-8 nativo, cero dep.
  - **Upload: `multer@2.1.1`** (NO 1.x: la v2 cerró los advisories de DoS de `busboy`). `memoryStorage` (el archivo nunca toca disco) + `limits { fileSize: 8MB, files: 1 }` + `fileFilter` por extensión.
- **`unpdf` es ESM-only y la API compila a CommonJS** (`apps/api` sin `"type":"module"`, tsconfig `NodeNext`). DECISIÓN: se carga con `await import('unpdf')` (dynamic import), que `tsc` NodeNext **preserva** en el output CJS (verificado en `dist`: no lo baja a `require`). `fflate`/`fast-xml-parser` (este último expone condición `require`) y `multer` se importan normal. Smoke test de runtime: txt, pptx (zip real) y pdf (mínimo válido) extraen OK; `import('unpdf')` resuelve desde el CJS compilado.
- **Reuso del pipeline (NO se reescribió):** nuevo `importService.extractPreviewFromFile({ filename, buffer, subjectName? })` detecta el tipo por **extensión** (el mimetype no es confiable), convierte a texto con el util `file-text.ts`, aplica el tope y llama al **mismo `extractPreview`** → mismo `extractTopics` (Gemini, Zod + dedup + cap≤50) → mismo `ImportPreview`. El `commit` (add/replace, el borrado lo decide el MODE) no se tocó: el preview del archivo es idéntico al del texto.
- **Topes y truncado-con-aviso (confirmado por el usuario):** archivo ≤ `MAX_IMPORT_FILE_BYTES` (8MB, multer). Texto extraído ≤ `MAX_IMPORT_FILE_TEXT_LENGTH` (50000 chars, **overrideable por la constante** en `@bract/shared`) — mayor que el de texto pegado (20000) porque un PDF/pptx rinde mucho más. Si se supera, se **trunca y se marca `truncated`** (campo aditivo y opcional en `ImportPreview`; el flujo por texto lo omite) → el frontend muestra un banner de aviso. **MEJORA FUTURA: chunking** para cobertura total de docs largos (partir el texto en bloques, extraer por bloque y unir/dedup) — hoy se procesa solo el primer bloque hasta el tope.
- **Errores manejados (sin códigos nuevos, set cerrado):** tipo no soportado / archivo sin texto (PDF escaneado) / archivo grande → `VALIDATION_ERROR` (400); falta `AI_API_KEY` → `AI_UNAVAILABLE` (503) propaga de `extractTopics`. Los errores de multer (LIMIT_FILE_SIZE) se traducen a `AppError` en un wrapper del middleware. La ruta `POST /import/topics/extract-file` usa multer (multipart) antes del controller; las otras siguen con el `express.json` global.
- **Frontend — paso 1 consolidado en `ImportSourceStep`** (reemplaza `ImportTextForm`): toggle "Pegar texto · Subir archivo" + **selector de materia destino compartido** (un solo `useSubjects`, sin duplicar). Modo archivo = dropzone con `accept`, validación de tipo/tamaño en cliente antes de subir. Tras extraer, ambas fuentes caen en el MISMO `ImportPreview` (que ahora muestra el banner de truncado). `api/`: `extractFile` arma `FormData` (axios pone el boundary). `hooks/`: mutación `extractFile` (no toca cache, como `extract`). 4 estados + i18n es/en completo (`import.form.source*/file*`, `import.preview.truncated`), sin hardcodear.
- **Sin env vars nuevas, sin modelos nuevos, sin `db push`.** Deps nuevas en `apps/api`: `multer@^2`, `unpdf`, `fflate`, `fast-xml-parser` + dev `@types/multer`; **`pnpm-lock.yaml` commiteado** (CI/Render usan `--frozen-lockfile`).
- **Tests:** `file-text.test.ts` (5: detección por extensión, txt/md decode, pdf con unpdf mockeado, pptx con zip sintético — runs por párrafo + orden + slide vacío) y `import.service.test.ts` (+4: tipo no soportado, sin texto, reuso del pipeline con subjectName, truncado). `pnpm -r typecheck` + `lint` + `test` (56/56) + `build` (api+web) verdes.
**Lección:** Para sumar una fuente de entrada nueva a un pipeline existente, convertí lo nuevo al formato que el pipeline ya consume (acá: archivo→texto) y enchufalo ANTES del primer paso, sin tocar el resto — el preview/commit quedaron intactos y el contrato solo creció con un `truncated?` opcional. Para parsers: preferí libs dedicadas con provenance claro (pdf.js de Mozilla vía unpdf; fflate+fast-xml-parser para el ZIP de pptx) sobre una mega-lib que arrastra su propio motor. Cuando una dep es ESM-only y compilás a CJS, `await import()` con NodeNext lo resuelve y `tsc` lo preserva — verificalo en `dist` y con un smoke test de runtime, no solo con el typecheck.

## [2026-06-13] Temario + estudio on-demand (§8.7, frontend, reusa C/D/E)
**Problema:** Dar agencia al estudiante para estudiar el tema que quiera, cuando quiera — un flujo centrado en el TEMA además del plan del día. Hoy se puede estudiar flashcards por tema y preguntarle al chat, pero está organizado por sección (planner/flashcards/chat), no por tema. Falta una sección Temario (overview navegable materia→tema) y, desde un tema, las herramientas on-demand juntas. Mayormente frontend, reusando lo existente — sin rehacer flashcards ni chat. Rama `agente-temario` (sin merge a main).
**Causa:** Feature "Temario + estudio on-demand" de `IDEAS_POST_MVP.md`. Reusa A (tipos/árbol), C (`useSubjects`/badges), D (flashcards) y E (chat). Spec-first en README §8.7 + Fase 13.
**Solución:** Feature nueva `features/syllabus/` + un refactor de reuso en flashcards + un deep-link aditivo al chat. **Cero backend nuevo.** Decisiones tomadas:
- **Refactor `StudyDeck` (reuso sin duplicar, sin regresión):** se extrajo de `flashcards/StudySession` un presentacional `StudyDeck` con TODO el flujo mostrar→revelar→calificar (snapshot local de la cola, índice, estados loading/empty/sessionDone). `StudySession` quedó como wrapper delgado sobre `useDueFlashcards` (pestaña "Estudiar" global **idéntica** en comportamiento y markup); el Temario lo envuelve sobre `useFlashcards(topicId)`. `StudyDeck` es genérico (`<T extends StudyCard>`) y recibe `getHeading` (cola due → tema/materia por carta; estudio por tema → tema fijo), copys de empty (varían: "todo repasado" vs "tema sin tarjetas") y `emptyAction` opcional (botón Generar con IA, solo en el Temario). La calificación sigue usando `review` (SM-2 persiste). Tests de flashcards (backend `srs`/`flashcard.service`) sin tocar y verdes — el refactor es de presentación.
- **Estudio on-demand = TODAS las cartas del tema (no la cola `due`):** el Temario usa `useFlashcards(topicId)` (no `useDueFlashcards`) para que estudiar un tema funcione aunque sus cartas estén pausadas en SRS (tema `PENDING`, efecto del Agente F). Es la esencia de la agencia: estudiar lo que querés cuando querés, sin esperar al schedule.
- **INTERACCIÓN CRUZADA CONOCIDA (aceptada, no se pelea):** calificar on-demand una carta de un tema pausado en SRS le mueve el `dueDate` de la centinela (año 9999, ver entrada de F) a `now+interval` → la reincorpora a la cola `due` global. Es coherente con "interactuar con un tema lo activa"; NO se intenta re-pausar (sería un caso borde sin valor para una app personal). Trade-off documentado; si en v2 se quiere aislamiento estricto, el Temario tendría que estudiar sin persistir SM-2 o re-pausar al salir.
- **Foco del chat por tema — SOLO frontend (no toca contrato ni streaming):** botón "Preguntar al chat" en el detalle del tema → `navigate('/chat', { state: { focusTopic } })`. `ChatPage` detecta el `focusTopic` (guard `useRef` + limpieza del state con `navigate(replace)` para no re-disparar), crea una sesión titulada con el tema y deja un `initialMessage` (i18n `chat.focusInitial`, nombra tema + materia). `ChatThread` recibe `initialMessage?`/`onInitialConsumed?` (props opcionales, aditivas) y lo auto-envía **una sola vez** vía el `send` existente (guard `initialSentRef`); se le puso `key={activeId}` para que cada sesión nueva remonte con el guard en false (auto-envío fiable en usos repetidos). El backend ya ensambla el árbol completo en cada mensaje, así que nombrar el tema basta para enfocar al tutor. El `focusTopic` real en `assembleStudentContext` queda como follow-up explícito.
- **Tipo del deep-link en el chat (`chat/types.ts`):** `ChatFocusTopic`/`ChatLocationState` exportados desde el barrel del chat; el Temario los importa para tipar el `state` de navegación. Dirección de dependencia syllabus→chat (natural: el Temario lanza el chat), sin ciclo.
- **Overview master/detail (mismo patrón que el chat):** `SyllabusPage` con `useSubjects` (fuente única del árbol, read-only — el CRUD vive en el planner) y los 4 estados; lista de materias en acordeón con `StatusBadge`/`DifficultyBadge` reusados; en mobile se muestra la lista o el detalle. `TopicDetailPanel` keyado por `topic.id` para resetear el estado interno (incluido el snapshot del deck) al cambiar de tema. Ruta lazy `/syllabus` con `ErrorBoundary` + entrada en sidebar (`nav.syllabus`, es **Temario** / en **Syllabus**). i18n es/en completo (bloque `syllabus.*` + `chat.focusInitial`), cero strings hardcodeados.
- **Sin env vars nuevas, sin modelos, sin `db push`, sin deps nuevas.** Reusa endpoints existentes de subjects/topics y flashcards. `pnpm -r typecheck` + `lint` + `test` (56/56) verdes.
**Lección:** Para un flujo "centrado en X" que reusa features existentes, extraé el núcleo presentacional (acá `StudyDeck`) a un componente genérico parametrizado por lo que varía (heading/copys/acciones) y dejá las pantallas viejas como wrappers delgados — se reusa sin duplicar y la pantalla original queda byte-idéntica (sin regresión). Un deep-link entre features se resuelve con el `state` de navegación de React Router + props opcionales aditivas en el destino + un guard `useRef` (y `key` para remonte fiable), sin tocar el contrato del backend. Cuando una feature on-demand pisa un efecto de dominio de otra (acá: reactivar una carta pausada en SRS), documentá la interacción y aceptala si es coherente con la intención del usuario, en vez de agregar complejidad para evitar un caso borde sin valor.

## [2026-06-13] Agente I — Evaluación / Quiz (corrección POR PREGUNTA server-side + anti-trampa)
**Problema:** Quiz de opción múltiple por tema o materia con la IA, con la respuesta correcta y la explicación por opción generadas en UNA sola llamada (sin 2da llamada al corregir), persistiendo los intentos (por-usuario) para habilitar después I-2 (puntos débiles). El primer diseño era "generate efímero + corrección local en el cliente"; el usuario lo **reemplazó** por **corrección por pregunta en el servidor con anti-trampa real**: el cliente no debe poder espiar la respuesta antes de elegir ni inflar el puntaje.
**Causa:** Feature "Agente I" de `IDEAS_POST_MVP.md`, encargada spec-first. Reusa A (Subject/Topic), B (`lib/ai`/Gemini). Rama `agente-i-quiz` (sin merge a main). Modelos nuevos → `db push` corrido por el usuario (Session pooler 5432). FUERA DE ALCANCE (follow-up **I-2**): dashboard agregado + detección de puntos débiles; este pase solo deja los datos persistidos.
**Solución:** Spec-first en README (§3.5 modelos, §5.5 rutas, §8.8 frontend, Fase 14). Modelos `QuizAttempt`/`QuizAttemptItem` + enums `QuizScope`/`QuizAttemptStatus`. Contrato en `@bract/shared` (tipos + Zod). `generateQuiz` aditivo en `lib/ai`. Módulo `apps/api/src/modules/quiz/` por capas + `features/quiz/` en el front. Decisiones tomadas:
- **DESVIACIÓN del diseño inicial (efímero → server-side por pregunta):** GENERAR es `POST /quiz/attempts`: llama a la IA PRIMERO (si falla, `AI_UNAVAILABLE` 503 y NO persiste nada), y si ok crea el `QuizAttempt(status=IN_PROGRESS)` + N `QuizAttemptItem` con `correctIndex` y `options` (con explicaciones) **AUTORITATIVOS en el server**. Al cliente solo viajan preguntas **PÚBLICAS** (`PublicQuizQuestion`: sin `correctIndex` ni `explanation`; opciones solo `text`). RESPONDER es `POST /quiz/attempts/:id/answers` `{ order, selectedIndex }`: el server corrige contra el `correctIndex` GUARDADO (un `selectedIndex` tramposo no infla — no hay `isCorrect` que el cliente pueda mandar) y recién ahí devuelve la reveal (`correctIndex` + explicaciones) **solo de esa pregunta**.
- **LOCK ATÓMICO anti-doble-respuesta (corrección pedida por el usuario):** el `recordAnswer` del repo hace el update **condicional** `updateMany({ where: { id, selectedIndex: null }, ... })`; si afecta 0 filas (otra request ganó la carrera), devuelve `false` y el service lo traduce a `CONFLICT` SIN recalcular nada. Así el lock es atómico en la DB (no solo un pre-check de lectura, que tenía una carrera teórica de inflar el puntaje en 1). `correctCount` se recalcula por `COUNT(isCorrect=true)` dentro de la misma transacción (no por increment → idempotente), y si no quedan preguntas sin responder se marca `status=COMPLETED` + `completedAt`.
- **Modelo (db push aditivo, tablas vacías → sin riesgo):** `QuizAttempt` agrega `status QuizAttemptStatus @default(IN_PROGRESS)` + `completedAt`. `QuizAttemptItem`: `selectedIndex Int?` (null = sin responder), `isCorrect Boolean @default(false)`. `userId` denormalizado en el item (§3.4) → índice `[userId, topicId]` y `[userId, topicId, isCorrect]` para I-2 (% de acierto por tema, sin join). Índice `[attemptId, order]` para buscar la pregunta a responder. FK a `Subject`/`Topic` con `onDelete: SetNull` (refs de agrupación/labels, NO ownership — eso es `userId`): borrar un tema/materia NO borra el historial; `scopeName` (snapshot) lo mantiene legible y el puntaje es un hecho histórico inmutable.
- **`generateQuiz` aditivo en `lib/ai` (mismo patrón que `extractTopics`):** `quizOutputSchema` (Zod) + `quizResponseSchema` (Gemini, OpenAPI-subset, sin `additionalProperties`), `QUIZ_SYSTEM` + `buildQuizUserPrompt`. Invariantes en código (la IA puede devolver basura): `correctIndex` en rango, opciones con texto+explicación, nº de opciones 2–6, `topicId ∈ temas de entrada` (si no, fallback al 1er tema → todos los topicId persistidos son del usuario, sin re-validar por item), dedup + cap. Inherente a IA: `AI_UNAVAILABLE` (503) sin key / salida inválida / 0 preguntas válidas.
- **Capas estrictas:** controller solo HTTP (envelope `{success,data,meta?}`), service recibe DTOs (nunca `req`) y hace grading + ownership + mappers (enums Prisma↔shared casteados acá, Date→ISO, `options` Json→tipo), repo solo Prisma. `[self]` con `authenticate` (sin gate de rol, scope por `req.user.id`). Sin N+1 (include explícito de items). `GET /quiz/attempts` lista solo COMPLETED; `GET /quiz/attempts/:id` trae items completos.
- **Frontend `features/quiz/`:** tabs "Nuevo quiz / Historial". Setup (RHF+Zod, `useSubjects` fuente única) → Runner una pregunta a la vez (POST answer → reveal del server; la correcta/explicación NO están en el cliente hasta responder) → Resultados (puntaje X/N + repaso) → Historial (lista + detalle revisable). Presentacional `QuestionReview` reusado (runner/resultados/detalle). 4 estados por vista, 503 amable (`error.code` del envelope), `CONFLICT` con mensaje propio. i18n es/en (`nav.quiz` + bloque `quiz.*`), ruta lazy `/quiz` con `ErrorBoundary`, sidebar (es **Evaluación** / en **Quiz**). Colores solo de tokens; sin `console.log`/`any`.
- **Sin env vars nuevas, sin deps nuevas.** `AI_API_KEY` ya existe (B); `db push` corrido por el usuario (modelos nuevos). `pnpm -r typecheck` + `lint` + `test` (71/71) + build api + build web verdes.
**Lección:** Para "anti-trampa real" en un quiz, la respuesta correcta y la explicación NUNCA deben viajar al cliente antes de contestar: persistilas autoritativas en el server al generar, mandá solo preguntas públicas, y corregí contra el valor guardado (no contra nada que mande el cliente). El lock contra doble-respuesta tiene que ser **atómico en la DB** (`updateMany WHERE selectedIndex IS NULL` + "0 filas → CONFLICT"), no un pre-check de lectura, que deja una carrera teórica; y recalcular el puntaje por `COUNT` (no por increment) lo hace idempotente. Generar = llamar a la IA primero y persistir solo si responde, para no dejar intentos vacíos ante `AI_UNAVAILABLE`.

## [2026-06-19] Fiabilidad de IA — reintento ante errores transitorios + prompt de quiz acotado en materia entera
**Problema:** Las generaciones de IA (quiz, corrección de abiertas, flashcards) fallaban de forma INTERMITENTE en el free tier de Gemini. La causa real son cortes TRANSITORIOS del proveedor (rate limit 429 / 503 overload / red), no un bug: una sola llamada que toca uno de esos cortes degrada a `AI_UNAVAILABLE` aunque un reintento inmediato habría andado. Aparte, un quiz de "materia entera" mandaba a la IA hasta ~100 temas en el prompt → prompt enorme y más frágil en el free tier.
**Causa:** Diseño "una sola llamada, sin reintento" en `lib/ai` (Agente B). El `generateQuiz` armaba el input de la IA con la lista COMPLETA de temas de la materia.
**Solución:** Dos fixes aditivos y self-contained en `apps/api/src/lib/ai/ai.service.ts`, sin tocar el chat por streaming, contratos, modelos ni `db push`. Rama `fix/ai-reliability-retry-quiz-scope` (sin merge).
- **FIX 1 — reintento con backoff exponencial (solo NO-stream):** helper `withAIRetry(label, call)` que envuelve las llamadas `generateContent` de `generateQuiz`, `gradeOpenAnswer` y `generateFlashcards`. Backoff ~0.5s/1.5s/3s → hasta 3 reintentos (4 intentos). Distingue TRANSITORIO de DETERMINISTA: `isTransientAIError` reintenta status HTTP {408,425,429,500,502,503,504}, errores de red (`cause.code` ECONNRESET/ETIMEDOUT/… o "fetch failed") y mensajes de overload/rate-limit/quota/unavailable; NO reintenta nuestro `AppError` ni 4xx deterministas. La falta de key se chequea ANTES (no llega al reintento) y la salida inválida/no-parseable se valida DESPUÉS (`parseStructured` fuera del wrapper) → nunca se reintenta una salida basura. En `gradeOpenAnswer` el reintento queda ANTES del lock del service (ya era IA-primero: si agota reintentos, `AI_UNAVAILABLE` y el alumno reintenta sin consumir el lock). El chat por SSE (`streamChatReply`) NO se tocó.
- **FIX 2 — prompt de quiz acotado:** en `generateQuiz`, el scope/scopeName/topicCount se siguen derivando del set COMPLETO en el service (sin cambios). Solo al armar el input de la IA se capea la lista a una MUESTRA de `QUIZ_PROMPT_MAX_TOPICS` (20, siempre ≥ la cantidad de preguntas): `scopedInput` alimenta tanto el prompt como `validateAndCapQuiz` (la IA solo conoce esos topicId, así que la validación usa el mismo set). Materias de ≤20 temas quedan idénticas a hoy.
- **Tests (+8, 136/136 verdes):** reintento de 503/red con éxito al 2º intento (quiz, grade, flashcards); 429 persistente agota reintentos → `AI_UNAVAILABLE` con 4 llamadas; 400 determinista y salida inválida → 1 sola llamada (sin reintento), con fake timers para no esperar el backoff real. FIX 2: 100 temas → el prompt lleva solo los primeros 20 ids. `pnpm -r typecheck` + `lint` + `test` verdes.
**Lección:** Un error de proveedor de IA no es binario: separá TRANSITORIO (rate limit/5xx/red → reintentá con backoff exponencial) de DETERMINISTA (sin key, 4xx, salida que no parsea → fallá ya, reintentar no cambia nada). Poné el reintento SOLO alrededor de la llamada de red, con el parseo/validación POR FUERA, para no reintentar una salida basura; y para flujos con lock, dejá el reintento ANTES del lock para que un corte transitorio no queme el intento del usuario. Para acotar un prompt que escala con los datos (materia entera), capeá lo que ve la IA a una muestra sin tocar lo que el dominio deriva del set completo.

## [2026-06-21] Quiz MIXTO perdía TODAS las MCQ — Gemini omitía `correctIndex` por no estar en `required` del responseSchema
**Problema:** Al generar un quiz MIXTO (MCQ + abiertas, p.ej. 5 totales con 2 abiertas) se persistían y mostraban **solo las abiertas**; las de opción múltiple desaparecían. Los quizzes de un solo tipo (todo MCQ o todo OPEN) funcionaban bien. SÍNTOMA SILENCIOSO: el endpoint respondía **201** (no 503/error), el quiz se renderizaba sin las MCQ. Evidencia de base: un intento quedó con `totalCount=2` y los 2 items `type=OPEN`, cero MCQ.
**Causa (CONFIRMADA POR DATOS, no por intuición):** Se instrumentó la cadena con logging temporal `[QUIZDEBUG]` (counts + motivos de descarte, sin contenido de preguntas) y se reprodujo un quiz mixto. En la llamada SOLO-MCQ los logs mostraron `aiReturned:3` / `kept:0` / `dropCounts:{mcq_correctIndex_invalid:3}`: **Gemini devolvía las 3 MCQ con 4 opciones cada una, pero SIN `correctIndex`**, y `validateAndCapQuiz` las descartaba a todas. La raíz: en `ai.schemas.ts`, el `responseSchema` de Gemini (structured output, OpenAPI-subset) tenía `correctIndex` como **propiedad** pero **NO en `required`** (solo `['type','topicId','question']`). El modelo lo omitía legalmente; Zod lo dejaba pasar (`correctIndex` opcional); el invariante de código lo dropeaba → 0 MCQ, silencioso, 201.
**Dos intentos previos que NO eran la causa (y por qué):**
- **Intento 1 — relajar `validateAndCapQuiz` (commit `5fa9edb`):** una opción MCQ sin `explanation` ya no descarta la pregunta entera (se conserva con `explanation=''`). Atacaba un problema REAL pero DISTINTO (un gate latente sobre la explicación), no el de `correctIndex`. No movió el síntoma: las MCQ seguían cayendo por `correctIndex` ausente, aguas abajo de ese relax.
- **Intento 2 — partir la generación en dos llamadas (commit `5af2bfa`):** ante el quiz mixto, hacer una llamada SOLO-MCQ y otra SOLO-OPEN en paralelo (`Promise.all`) en vez de una sola pidiendo ambos tipos. Correcto y necesario como ANDAMIO (flash-lite mezcla mal MCQ+OPEN en una llamada), pero insuficiente: ambas llamadas seguían usando el MISMO `responseSchema` unificado sin `correctIndex` en `required`, así que la llamada SOLO-MCQ devolvía 3 MCQ sin índice y `validateAndCapQuiz` las descartaba igual. Sin datos, parecía "arreglado" pero no lo estaba — por eso se instrumentó antes de tocar nada.
**Solución (fix definitivo, commit `6b5b069`, rama `fix/quiz-mcq-response-schema` ff-merged a main):** Aprovechando que la generación YA está partida por tipo, cada llamada usa un `responseSchema` ESPECÍFICO que pone en `required` exactamente lo que ese tipo necesita.
- **`ai.schemas.ts` — schema por tipo:** se reemplaza el `quizResponseSchema` unificado por `quizMcqResponseSchema` (`required: ['type','topicId','question','options','correctIndex']` → fuerza a Gemini a emitir `correctIndex` Y `options`) y `quizOpenResponseSchema` (`required: [...,'expectedAnswer']`, sin `options`/`correctIndex`).
- **`ai.service.ts` / `runQuizGeneration` — selección por tipo:** `const responseSchema = openCap === 0 ? quizMcqResponseSchema : quizOpenResponseSchema`. Cada llamada de `generateQuiz` pide UN solo tipo (la mixta se parte en dos; los single-type ya lo eran), así que `openCap` discrimina sin ambigüedad.
- **`quizOptionSchema.explanation` relajada a opcional (`z.string().default('')`):** desactiva un gate LATENTE (era `.min(1)`): si la IA emitía una sola `explanation` vacía, fallaba el parse Zod del quiz ENTERO → 503. Coherente con el relax del Intento 1: lo obligatorio en una MCQ es `options` + `correctIndex` válido, no la explicación (la explicación se sigue pidiendo en el prompt y en el schema de Gemini para calidad, pero su ausencia ya no rompe nada).
- **Instrumentación `[QUIZDEBUG]` removida** una vez confirmada la causa (3 logs + array `drops` + 5º parámetro `label` de `validateAndCapQuiz`; el `label` de `runQuizGeneration` se conserva, lo usa `withAIRetry`).
- **Tests (TDD, +2 RED→GREEN, 179/179 verdes):** (a) cada llamada usa el schema correcto — la SOLO-MCQ exige `options`+`correctIndex` en `required`, la SOLO-OPEN exige `expectedAnswer` (falló antes con `['type','topicId','question']`); (b) una opción MCQ sin `explanation` ya no rompe el parse, se conserva con `explanation=''` (antes tiraba `AI_UNAVAILABLE`/503). `typecheck` + `lint` + `test` verdes.
**Lección:** En structured outputs de Gemini (OpenAPI-subset), un campo que está en `properties` pero NO en `required` el modelo lo puede OMITIR — y si tu validación de negocio lo exige, descarta la entidad entera de forma SILENCIOSA (201 con datos faltantes, sin 503). No alcanza con validar en código: poné en `required` del responseSchema lo que el negocio necesita SÍ o SÍ. Cuando la generación está partida por tipo, usá un schema POR tipo (no uno unificado laxo) para forzar exactamente los campos de cada uno. Y, sobre todo: ante un síntoma silencioso con dos fixes previos fallidos, NO adivines — instrumentá el punto exacto de pérdida (counts + motivo de descarte), reproducí UNA vez y leé la causa en los datos antes de tocar nada (`aiReturned:3 / kept:0 / mcq_correctIndex_invalid:3` señaló la raíz sin ambigüedad). Distinguí el ANDAMIO necesario (partir en dos llamadas) de la CAUSA RAÍZ (el `required` del schema): el primero era condición previa, el segundo el arreglo real.
